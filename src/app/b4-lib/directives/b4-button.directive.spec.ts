import { B4ButtonDirective } from './b4-button.directive';

describe('B4ButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new B4ButtonDirective();
    expect(directive).toBeTruthy();
  });
});
